services:
  - type: web
    name: proof-ai-demo
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python demo_server.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
