# ── PROOF-AI Demo — Production Dependencies ───────────────────────────────
# Core Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=21.2.0
pydantic>=2.9.0

# Core AI/LLM Integration (required by fact_checker.py)
litellm>=1.45.0
numpy>=1.26.0

# Note: sentence-transformers is NOT needed at runtime.
# The NLI verifier uses LLM-based proxy, not local CrossEncoder.
